@page
@model ConfirmEmailModel
@{
    ViewData["Title"] = "Confirm email";
}
@using Microsoft.AspNetCore.Identity
@using ITMatching.Models

@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager
@inject ITMatchingAppContext AppContext
<div class="congrats-overlay">
    <center>
        <h1 >Congratulations, You Have Completed Your Registeration!</h1>
        <br />
        <br />
    </center>
    <div class="row justify-content-center">
        <div class="">
            <a class="Tempbtn" asp-area="Identity" asp-page="/Account/Register">Go To My Profile Page</a>
        </div>
        <div class="">
            <a class="Tempbtn" asp-area="Identity" asp-page="/Account/ExpertRegister">Set Up my Schedule</a>
        </div>
    </div>

</div>


@* @if (SignInManager.IsSignedIn(User))
    {
        string id = UserManager.GetUserId(User);

        Itmuser ituser = AppContext.Itmusers.Where(u => u.AspNetUserId == id).FirstOrDefault();
        Expert expert = AppContext.Experts.Where(u => u.ItmuserId == ituser.Id).FirstOrDefault();
        if (expert != null)
        {
            <div style="margin-top: 100px;">
                <a class="Tempbtn" asp-area="Identity" asp-page="/Account/Schedular">Set Your Work Schedule</a>
            </div>
        }
    }*@

